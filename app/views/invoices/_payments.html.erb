<fieldset class="pull-right total_1">
    <table>
            <tr>
                 <%if params[:action] != 'new'  %>
                         <td><%=f.input :created_at,  value: DateTime.now, as: :string, :format=> :default, readonly: :true  %></td>
                         <% end %>
                <td><%=f.input :amount, :label =>'Pagos'  %></td>

          <td><%= f.hidden_field :_destroy %></td>
          <td><%= button_to "Eliminar", '#', class: "remove_fields_payment btn btn-danger" %></td>
        </tr>
    </table>
</fieldset>
